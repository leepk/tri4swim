# Existing PostgreSQL server connection.
# If PostgreSQL runs on the Docker host (Mac/Windows), use host.docker.internal.
# If PostgreSQL is remote, replace the host with its hostname/IP.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tri4swim?schema=public"

NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Email (optional until SMTP is configured)
EMAIL_FROM="Tri4Swim <noreply@tri4swim.com>"
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=tri4swim@gmail.com
SMTP_PASS=qicqrpblcfzcycto
ADMIN_EMAIL=

# Admin login - change these before production
ADMIN_PASSWORD="tri4swim@admin123"
ADMIN_SESSION_SECRET="tri4swim-change-this-secret-before-production"
