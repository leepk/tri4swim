'use client';import {FormEvent,useState} from 'react';import {useRouter} from 'next/navigation';
export default function Login(){const [error,setError]=useState(''),[busy,setBusy]=useState(false);const router=useRouter();async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget);const r=await fetch('/api/admin/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({password:f.get('password')})});const d=await r.json();setBusy(false);if(!r.ok)return setError(d.error||'Unable to sign in');router.replace('/admin');router.refresh()}return <main className="login-page"><form className="login-card" onSubmit={submit}><img src="/logo.png" alt="Tri4Swim"/><span className="eyebrow">COACH PORTAL</span><h1>Welcome back</h1><p>Sign in to manage lessons, bookings and your coaching calendar.</p><label>Password<input name="password" type="password" autoComplete="current-password" required autoFocus/></label>{error&&<div className="login-error">{error}</div>}<button className="btn" disabled={busy}>{busy?'Signing in…':'Sign in'}</button><a href="/">← Back to website</a></form></main>}
