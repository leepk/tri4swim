import {NextRequest,NextResponse} from 'next/server';import {prisma} from '@/lib/db';import {ADMIN_COOKIE,validSession} from '@/lib/admin-auth'
const ok=(r:NextRequest)=>validSession(r.cookies.get(ADMIN_COOKIE)?.value)
export async function GET(r:NextRequest){if(!ok(r))return NextResponse.json({error:'Unauthorized'},{status:401});return NextResponse.json(await prisma.siteContent.findUnique({where:{id:1}}))}
export async function PATCH(r:NextRequest){if(!ok(r))return NextResponse.json({error:'Unauthorized'},{status:401});const b=await r.json();const allowed=['heroEyebrow','heroTitle','heroText','heroImage','coachName','coachTitle','coachBio','coachImage','email','phone','location','bookingTitle','bookingText','benefit1Title','benefit1Text','benefit2Title','benefit2Text','benefit3Title','benefit3Text','lessonsTitle','coachSectionTitle','ctaTitle','ctaText'];const data:any={};for(const k of allowed)if(typeof b[k]==='string')data[k]=b[k];return NextResponse.json(await prisma.siteContent.upsert({where:{id:1},create:{id:1,...data},update:data}))}
